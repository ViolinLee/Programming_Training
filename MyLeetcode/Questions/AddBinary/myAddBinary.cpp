#include <string.h>

using namespace std;
class Solution {
public:
    string addBinary(string a, string b) {
        size_a = a.size();
        size_b = b.size();
        size_max = max(a, b); size_min = min(a,b);
        
        if size_a = size_max;
        
        
        string c;
        int carry = 0;
        int tmp;
        for (int i = size_max-1; i >= 0; i--) {
        	if (i <= size_min) tmp = (int) a[i] + (int) b[i] + carry;
        	else tmp = 
		}
    }
};

// ×¢£ºÊäÈëÊÇ×Ö·û´® 
