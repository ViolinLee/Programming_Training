/*
1.数据成员： 
    firstNode
	listSize 
2.方法： 
    构造函数 + 拷贝构造 + 析构函数 
    bool empty()
    int size()
    T& get(int theIndex) const
	int indexOf() 
	void insert()
	void erase()  
	void output()
3.嵌套类：
    class iterator 
4.注意：
    erase() 而不是 delete()
	#include 了那些头文件？ 
*/

// chain

#ifndef
#define

#include "chainNode.h"
#include <linearList>




#endif
